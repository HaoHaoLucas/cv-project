# 经过验证的 pip 依赖（base conda 环境，Python 3.10.13，torch 2.1.2+cu118）
#
# 安装顺序：
#   1. pip install torch==2.1.2 torchvision==0.16.2 --index-url https://download.pytorch.org/whl/cu118
#   2. pip install -r requirements.txt
#
# 注意：
#   - numpy 必须 < 2.0（torch 2.1.2 使用 NumPy 1.x ABI）
#   - transformers 必须 < 5.0（5.x 要求 PyTorch >= 2.4）

transformers==4.47.1
timm>=0.9.12
tokenizers>=0.19.0
einops>=0.7.0
pycocotools>=2.0.7
opencv-python>=4.9.0
Pillow>=10.2.0
numpy<2.0
scipy>=1.12.0
matplotlib>=3.8.0
supervision>=0.19.0
addict>=2.4.0
yapf>=0.40.0
tqdm>=4.66.0
pyyaml>=6.0.1
ipykernel>=6.29.0
ipywidgets>=8.1.0
