# WSL 复现提示（已合并）

WSL 与云服务器共用同一份复现计划，见：

**[高精度复现计划_OVD_VG.md](./高精度复现计划_OVD_VG.md)**

- **WSL**：按该文档执行；CUDA 算子编译常失败，可先用 PyTorch fallback 跑通主线。  
- **云服务器（Ubuntu 22 + V100）**：优先阅读文档 **§0 云服务器快速上手** 与 **§11 给 Agent 的提示**。
